<ContentDialog
    x:Class="UI.Views.DeleteConfirmation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="using:UI.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="Yes"
    SecondaryButtonText="No"
    PrimaryButtonCommand="{Binding DeleteCommand}">

    <ContentDialog.DataContext>
        <vm:DeleteConfirmationViewModel x:Name="ViewModel"/>
    </ContentDialog.DataContext>

    <Grid>
        <TextBlock Text="Are you sure want to delete this Envelope?"
                   FontSize="25"
                   Padding="5"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
    </Grid>
</ContentDialog>

